# Process logs

Tracked scientific logs use `YYYY-MM-DD_<task>.md` and contain the objective,
input snapshot, exact command, QC, results, limitations, decisions, and next
action. Machine stdout/stderr and manifests remain under the gitignored
`logs/<run_id>/` tree.

